# Traefik demo

Traefik docker compose demonstration.

```bash
docker compose up -d
```

Access the API:
- api-hello1: [api.localhost/v1](http://api.localhost/v1)
- api-hello2: [api.localhost/v2](http://api.localhost/v2)

Traefik
- dashboard: [http://api.localhost:8080](http://api.localhost:8080)
